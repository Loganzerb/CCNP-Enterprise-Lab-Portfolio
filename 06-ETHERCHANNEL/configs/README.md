# Configuration captures

These files are verbatim, de-indented copies of the four IOSvL2 configuration blocks embedded in `../CML-LAB.yaml`. The YAML export remains the topology/configuration source of truth; the separate files exist only to make each device configuration easier to review.
